#ifndef MACRO_H
#define MACRO_H

#define SP SortPriority
#define MIN(a, b) (((a) < (b)) ? (a) : (b))
#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#define ABS(a) (((a) < 0) ? (-(a)) : (a))
typedef unsigned long long ULL;
typedef long long LL;
typedef unsigned int UI;
//typedef unsigned long size_t;
//#define nullptr 0

#endif // MACRO_H
